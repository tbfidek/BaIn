//se pun datele locale de conectare la baza de date
const database = {
    user: 'postgres',
    host: 'localhost',
    database: 'TW',
    password: 'daria',
    port: 5432,
};

export default database;
